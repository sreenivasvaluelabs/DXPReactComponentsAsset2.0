# Environment Configuration

# Server Configuration (Full-Stack Mode)
NODE_ENV=development
PORT=5000

# Database Configuration (Full-Stack Mode)
# Replace with your actual database URL
# Example for PostgreSQL: postgresql://username:password@localhost:5432/database_name
DATABASE_URL=postgresql://username:password@localhost:5432/dxp_components

# Frontend Environment Variables (Vite - Frontend Only Mode)
# These are included in the frontend build and accessible via import.meta.env
VITE_APP_NAME=DXP React Components Asset
VITE_API_URL=http://localhost:5000/api
VITE_ENABLE_ANALYTICS=false

# Development Configuration
REPL_ID=